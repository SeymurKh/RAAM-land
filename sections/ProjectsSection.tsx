"use client"; import { useEffect, useMemo, useState } from "react"; import Image from "next/image"; import { AnimatePresence, motion } from "framer-motion"; import { ArrowUpRight, ChevronLeft, ChevronRight, Play } from "lucide-react"; import { MotionReveal } from "@/components/MotionReveal"; import { SectionFrame } from "@/components/SectionFrame"; import { YouTubeModal } from "@/components/YouTubeModal"; import type { Project } from "@/types/content"; export function ProjectsSection() { const [projects, setProjects] = useState<Project[]>([]); const [activeIndex, setActiveIndex] = useState(0); const [modalState, setModalState] = useState<{ url: string; title: string; } | null>(null); useEffect(() => { fetch("/api/projects") .then((response) => response.json()) .then((data: Project[]) => { setProjects([...data].sort((a, b) => a.order - b.order)); }); }, []); const activeProject = projects[activeIndex]; const paddedIndex = useMemo( () => String(activeIndex + 1).padStart(2, "0"), [activeIndex], ); const paddedTotal = useMemo( () => String(projects.length).padStart(2, "0"), [projects.length], ); function goTo(direction: 1 | -1) { setActiveIndex((current) => { if (projects.length === 0) { return 0; } return (current + direction + projects.length) % projects.length; }); } function openProject(project: Project | undefined) { if (!project?.youtubeUrl) { return; } setModalState({ url: project.youtubeUrl, title: project.title }); } function handleKeyDown(event: React.KeyboardEvent<HTMLElement>) { if (event.key === "ArrowLeft") { event.preventDefault(); goTo(-1); } if (event.key === "ArrowRight") { event.preventDefault(); goTo(1); } if (event.key === "Enter") { openProject(activeProject); } } return ( <SectionFrame id="projects" eyebrow="Projects" intro="A focused gallery of the formats that currently define RAAM's public platform." > <section tabIndex={0} onKeyDown={handleKeyDown} className="outline-none" aria-label="Projects gallery" > {projects.length === 0 ? ( <MotionReveal className="flex min-h-[360px] items-center justify-center rounded-[1.5rem] border border-white/10 bg-[#0b0a09]/86 p-8 text-center"> <p className="text-sm uppercase tracking-[0.28em] text-stone-300/55"> Projects will appear here soon </p> </MotionReveal> ) : ( <div className="relative mx-auto max-w-5xl"> <div className="pointer-events-none absolute -inset-4 rounded-[2rem] border border-white/5 bg-black/20 blur-xl" /> <div className="relative overflow-hidden rounded-[1.35rem] border border-white/12 bg-[#090909]/88 shadow-[0_28px_120px_rgba(0,0,0,0.55)] backdrop-blur-xl"> <div className="absolute inset-0 bg-[radial-gradient(circle_at_18%_15%,rgba(255,255,255,0.12),transparent_24%),linear-gradient(135deg,rgba(255,255,255,0.08),transparent_36%,rgba(255,255,255,0.04))]" /> <AnimatePresence mode="wait" initial={false}> <motion.article key={activeProject.id} drag="x" dragConstraints={{ left: 0, right: 0 }} dragElastic={0.08} onDragEnd={(_, info) => { if (info.offset.x > 80) { goTo(-1); } if (info.offset.x < -80) { goTo(1); } }} initial={{ opacity: 0, x: 48, filter: "blur(10px)" }} animate={{ opacity: 1, x: 0, filter: "blur(0px)" }} exit={{ opacity: 0, x: -48, filter: "blur(10px)" }} transition={{ duration: 0.45, ease: [0.22, 1, 0.36, 1] }} className="relative grid min-h-[560px] gap-8 p-5 sm:p-8 lg:grid-cols-[0.9fr_1.1fr] lg:p-10" > {/* Image zone with fixed aspect ratio */} <div className="flex flex-col justify-between rounded-[1rem] border border-white/10 bg-black/30 p-5 sm:p-6"> {activeProject.image ? ( <div className="relative mb-5 aspect-[4/3] w-full overflow-hidden rounded-lg border border-white/8"> <Image src={activeProject.image} alt={activeProject.title} fill className="object-cover" sizes="(max-width: 1024px) 100vw, 45vw" /> </div> ) : ( <div className="mb-5 flex aspect-[4/3] w-full items-center justify-center rounded-lg border border-white/8 bg-white/[0.03]"> <span className="text-xs uppercase tracking-[0.28em] text-stone-300/35"> No image </span> </div> )} <div> <p className="text-xs uppercase tracking-[0.42em] text-stone-300/50"> {activeProject.category} </p> <h3 className="mt-4 max-w-xl text-3xl font-semibold uppercase leading-[0.9] tracking-normal text-stone-50 line-clamp-2 sm:text-4xl lg:text-5xl"> {activeProject.title} </h3> </div> <div className="mt-4 flex flex-wrap gap-2"> <span className="rounded-full border border-white/10 px-3 py-1 text-[0.65rem] uppercase tracking-[0.2em] text-stone-200/64"> {activeProject.status} </span> <span className="rounded-full bg-stone-100/8 px-3 py-1 text-[0.65rem] uppercase tracking-[0.2em] text-stone-200/64"> {activeProject.accent} </span> </div> </div> <div className="flex flex-col justify-between"> <div className="space-y-5 text-base leading-8 text-stone-100/72 sm:text-lg sm:leading-9 line-clamp-6"> {activeProject.description.map((paragraph) => ( <p key={paragraph}>{paragraph}</p> ))} </div> <div className="mt-auto flex flex-col gap-5 pt-10 sm:flex-row sm:items-end sm:justify-between"> <button type="button" onClick={() => openProject(activeProject)} disabled={!activeProject.youtubeUrl} className="group inline-flex h-14 items-center justify-center gap-3 rounded-full border border-white/12 bg-white/[0.08] px-6 text-sm font-medium uppercase tracking-[0.22em] text-white transition hover:bg-white/[0.14] disabled:cursor-not-allowed disabled:opacity-45" > <Play size={16} /> Playlist <ArrowUpRight size={15} className="transition group-hover:translate-x-0.5 group-hover:-translate-y-0.5" /> </button> <div className="flex items-center justify-between gap-4 sm:justify-end"> <span className="text-xs uppercase tracking-[0.28em] text-stone-300/45"> {paddedIndex} / {paddedTotal} </span> <div className="flex gap-2"> <button type="button" onClick={() => goTo(-1)} className="inline-flex h-12 w-12 items-center justify-center rounded-full border border-white/10 bg-black/25 text-white transition hover:bg-white/10" aria-label="Previous project" > <ChevronLeft size={18} /> </button> <button type="button" onClick={() => goTo(1)} className="inline-flex h-12 w-12 items-center justify-center rounded-full border border-white/10 bg-black/25 text-white transition hover:bg-white/10" aria-label="Next project" > <ChevronRight size={18} /> </button> </div> </div> </div> </div> </motion.article> </AnimatePresence> </div> <div className="mt-5 flex flex-wrap justify-center gap-2"> {projects.map((project, index) => ( <button key={project.id} type="button" onClick={() => setActiveIndex(index)} className={`h-2.5 rounded-full transition ${ index === activeIndex ? "w-10 bg-stone-100" : "w-2.5 bg-stone-100/28 hover:bg-stone-100/55" }`} aria-label={`Show ${project.title}`} /> ))} </div> </div> )} </section> <YouTubeModal youtubeUrl={modalState?.url} title={modalState?.title ?? ""} isOpen={!!modalState} onClose={() => setModalState(null)} /> </SectionFrame> ); }